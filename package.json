{
  "name": "TODO",
  "description": "TODO",
  "version": "0.0.1",
  "license": "MIT",
  "main": "src/electron.js",
  "scripts": {
    "dev": "concurrently \"sapper dev\" \"wait-on http://localhost:3000 && cross-env NODE_ENV=development electron .\"",
    "build": "sapper build --legacy",
    "export": "sapper export --legacy",
    "start": "concurrently \"cross-env BROWSER=none NODE_ENV=development node __sapper__/build\" \"wait-on http://localhost:3000 && cross-env NODE_ENV=development electron .\"",
    "startprod": "electron .",
    "cy:run": "cypress run",
    "cy:open": "cypress open",
    "test": "run-p --race dev cy:run",
    "package": "electron-packager . pos --overwrite --platform=win32 --arch=ia32 --icon=static/icons/win/icon.ico --prune=true --out=dist --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"POS application\""
  },
  "dependencies": {
    "compression": "^1.7.1",
    "polka": "^0.5.0",
    "sirv": "^0.4.0"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0",
    "@babel/plugin-syntax-dynamic-import": "^7.0.0",
    "@babel/plugin-transform-runtime": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "@babel/runtime": "^7.0.0",
    "concurrently": "^4.0.1",
    "cross-env": "^5.2.0",
    "electron": "5.0.1",
    "npm-run-all": "^4.1.5",
    "rollup": "^1.0.0",
    "rollup-plugin-babel": "^4.0.2",
    "rollup-plugin-commonjs": "10.0.0",
    "rollup-plugin-node-resolve": "5.0.0",
    "rollup-plugin-replace": "^2.0.0",
    "rollup-plugin-svelte": "^5.0.1",
    "rollup-plugin-terser": "^4.0.4",
    "sapper": "^0.27.0",
    "svelte": "^3.0.0",
    "wait-on": "^3.2.0"
  }
}
